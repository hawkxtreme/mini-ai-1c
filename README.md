# Mini AI 1C

> [!WARNING]
> ⚠️ **Данное приложение находится в стадии активной разработки.**
> Возможны ошибки и изменения в функционале. Мы активно работаем над улучшением стабильности и добавлением новых возможностей.

**Mini AI 1C** — это ИИ-ассистент, разработанный специально для разработчиков **1C:Предприятие**. Он интегрирует возможности современных LLM прямо в ваш рабочий процесс, позволяя генерировать, анализировать и применять код BSL (1C:Предприятие) быстро и удобно.

![Демонстрация работы](/img/WdK6tGx0Ld.gif)

## 🚀 Возможности

- **ИИ-чат с контекстом**: Обсуждайте ваш код с ИИ, который понимает синтаксис и контекст BSL.
- **Интеграция с Конфигуратором 1С (Windows)**:
  - **Получить код**: Мгновенно забирайте текст модуля или выделенный фрагмент из активного окна Конфигуратора.
  - **Применить изменения (Apply)**: Генерируйте изменения кода и применяйте их обратно в Конфигуратор одним кликом.
- **Продвинутый редактор кода**:
  - **Режим сравнения (Diff)**: Удобное визуальное сравнение предложенных ИИ изменений с оригинальным кодом.
  - **Валидация кода**: Встроенный BSL Language Server обеспечивает подсветку синтаксиса и диагностику ошибок в реальном времени.
  - **Свертывание кода (Folding)**: Поддержка сворачивания процедур, функций, циклов, условий и областей (`#Область`).
  - **Изменение размера**: Гибкая боковая панель с редактором, которую можно растягивать или сворачивать.
- **Авто-исправление кода (Self-Healing)**: Если ИИ генерирует код с ошибками, приложение автоматически отправляет его на исправление (до 3 попыток).
- **Window Persistence**: Приложение запоминает размер, положение окна и выбранный монитор при перезапуске.
- **Современный UI**: Создан на базе Tauri 2 и React 19 для максимальной производительности и нативного ощущения.

## 🛠 Технологический стек

- **Frontend**: React 19, TypeScript, TailwindCSS, Vite, Monaco Editor
- **Backend/Core**: Tauri 2 (Rust)
- **Language Server**: BSL Language Server (встроенная интеграция через WebSocket)
- **AI Integration**: OpenAI-совместимый API с поддержкой реестра популярных моделей (Anthropic, DeepSeek, OpenRouter и др.)

## 📋 Требования

Перед началом работы убедитесь, что у вас установлено следующее:

1.  **Node.js** (v18 или новее) и **npm**
2.  **Rust** (последняя стабильная версия) - [Установить Rust](https://www.rust-lang.org/tools/install)
3.  **Java Runtime Environment (JRE)** (v17 или новее) - Необходима для работы BSL Language Server.
4.  **Windows 10/11** (для полноценной работы функций интеграции с Конфигуратором 1С)

## ⚡ Установка и Запуск (Development)

1.  **Клонируйте репозиторий**
    ```bash
    git clone https://github.com/hawkxtreme/mini-ai-1c
    cd mini-ai-1c/tauri-app
    ```

2.  **Установите зависимости**
    ```bash
    npm install
    ```

3.  **Запустите в режиме разработки**
    ```bash
    npm run app:dev
    ```
    Или через полное имя: `npm run mini-ai-1c dev`.
    Эта команда запустит сервер разработки фронтенда и бэкенд Tauri.

## 📦 Сборка (Production)

Чтобы создать готовый исполняемый файл (exe):

```bash
npm run app:build
```
Или через полное имя: `npm run mini-ai-1c build`.
Файлы сборки будут находиться в папке `src-tauri/target/release/bundle`.

## 🔧 Настройка

Приложение использует локальный профиль для хранения ключей API и настроек.
- Нажмите иконку **Настройки** (шестеренка) в приложении, чтобы настроить провайдера LLM (например, OpenAI, Anthropic или локальные модели).

## 📄 Лицензия

Этот проект распространяется под лицензией **MIT License**. Подробности смотрите в файле `LICENSE`.

---
*Создано с ❤️ для сообщества 1С*
